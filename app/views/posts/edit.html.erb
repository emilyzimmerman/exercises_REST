<%= form_with(model: @post) do |form|%>

<div>
    <%= form.label :title %>
    <%= form.text_field :title %>
</div>

<div>
    <%= form.label :body%>
    <%= form.text_area :body %>
</div>

<div>
    <%= form.submit "Update"%>
</div>

<% end %>

<%= link_to "Back to Index", posts_path %>